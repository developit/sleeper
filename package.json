{
  "name": "sleeper",
  "version": "0.1.0",
  "homepage": "https://github.com/developit/sleeper",
  "author": "Jason Miller <jason@developit.ca>",
  "description": "A high-level network abstraction that makes working with REST APIs simple.",
  "main": "sleeper.js",
  "scripts": {
    "build": "npm run transpile && npm run size",
    "transpile": "babel src -s -d .",
    "size": "echo \"gzip size: $(pretty-bytes $(gzip-size sleeper.js))\"",
    "test": "mocha-phantomjs test/index.html",
    "prepublish": "npm run build",
    "release": "npm run build && git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags && npm publish"
  },
  "keywords": [
    "rest",
    "jan",
    "network",
    "model",
    "ajax"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/developit/sleeper.git"
  },
  "licence": "GPLv3",
  "devDependencies": {
    "babel": "^5.8.21",
    "gzip-size": "^3.0.0",
    "mocha-phantomjs": "^3.6.0",
    "phantomjs": "^1.9.7-15",
    "pretty-bytes": "^2.0.1"
  },
  "dependencies": {
    "jan": "^1.0.2"
  }
}
